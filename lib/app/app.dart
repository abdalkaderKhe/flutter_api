import 'package:api_tut/app/home/home_page.dart';
import 'package:flutter/material.dart';

class ApiExample extends StatelessWidget {
  const ApiExample({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: HomePage(),
    );
  }
}
